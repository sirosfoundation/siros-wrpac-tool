# Build output
/siros-wrpac-tool
/bin/

# Go
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out

# Dependency directories
vendor/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Test output
coverage.out
coverage.txt
coverage.html

# Private keys, never. Certificates are public and the git-driven workflow
# expects issued material to be committable, so *.pem is deliberately not
# blanket-ignored — only local scratch directories are.
*.key
/out/
/deployment/
