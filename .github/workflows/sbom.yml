name: SBOM

on:
  push:
    branches: [main]
  release:
    types: [published]

permissions:
  contents: read

jobs:
  sbom:
    uses: sirosfoundation/.github/.github/workflows/sbom.yml@main
    permissions:
      contents: write
      packages: read
      id-token: write
