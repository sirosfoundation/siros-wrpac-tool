# SonarCloud code quality and security analysis.
name: SonarCloud

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  pull-requests: read

jobs:
  sonarcloud:
    name: SonarCloud Scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Install SoftHSM and OpenSC
        run: sudo apt-get update && sudo apt-get install -y softhsm2 opensc

      # The softhsm tag is required: without it the PKCS#11 paths are excluded
      # from the build and Sonar reports them as uncovered.
      - name: Generate coverage report
        run: go test -tags softhsm -coverprofile=coverage.out ./...

      # Dependabot pull requests do not receive repository or organization
      # Actions secrets, so SONAR_TOKEN is empty there and the scanner exits 3.
      # This job is a required check, so a failing scan left every Dependabot
      # PR unmergeable. Skip the scan for them and let the job pass; the same
      # commits are scanned on the push to main after the merge.
      - name: Skip scan on Dependabot pull requests
        if: github.actor == 'dependabot[bot]'
        run: echo "::notice::SonarCloud scan skipped for a Dependabot pull request; it runs on main after merge"

      - name: SonarCloud Scan
        if: github.actor != 'dependabot[bot]'
        # Pinned by hash, not by tag: GHSA-f79p-9c5r-xg88 (command injection,
        # fixed in 5.3.1) and GHSA-5xq9-5g24-4g6f (argument injection, fixed in
        # 6.0.0) both affect the v5 line, and a moving tag would let a
        # compromised release replace this without the reference changing.
        uses: SonarSource/sonarqube-scan-action@22918119ff8e1ca75a623e15c8296b6ea4fbe28f # v8.2.1
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.organization=${{ github.repository_owner }}
            -Dsonar.projectKey=${{ github.repository_owner }}_${{ github.event.repository.name }}
            -Dsonar.go.coverage.reportPaths=coverage.out
            -Dsonar.tests=.
            -Dsonar.test.inclusions=**/*_test.go
